---
title: 系统命令
icon: Terminal
---

<Callout type="warn">
  此 API 较为底层，并且不跨平台，且可能不安全，不建议使用
</Callout>

```ts twoslash
/// <reference types="extprg-types" />
// ---cut---
const { code, stdout, stderr } = await prg.shell_execute(
  "tee",
  ["-a", "test.txt"],
  // stdin
  "hello world",
);
```
